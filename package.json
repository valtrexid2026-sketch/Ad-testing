{
  "name": "ad-testing",
  "private": true,
  "version": "1.0.0",
  "description": "Static video-player demo used to test ad placement.",
  "scripts": {
    "install": "echo \"✅ ad-testing: static site — no external dependencies to install\"",
    "dev": "python3 -m http.server 3000 --bind 0.0.0.0",
    "preview": "bun run dev",
    "start": "bun run dev",
    "build": "rm -rf dist && mkdir -p dist && cp index.html CNAME dist/ && cp logo-light.png dist/ 2>/dev/null || true"
  }
}
